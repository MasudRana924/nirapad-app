import firebase from '@react-native-firebase/app';

// Firebase is automatically configured via google-services.json
// No manual initialization needed when using google-services.json
export default firebase;
